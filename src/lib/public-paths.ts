// Marketing-adjacent pages with no logged-in-only content -- app-only UI
// (nav bar, keyboard shortcuts) shouldn't activate on these for logged-out
// visitors.
export const PUBLIC_PATHS = ["/", "/sign-in", "/sign-up", "/privacy", "/terms", "/contact"];
